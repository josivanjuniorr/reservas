// Copy this file to `config.js` (do NOT commit `config.js`).
// Fill the `url` and `anonKey` from your Supabase project settings.
window.SUPABASE_CONFIG = {
  url: 'https://your-project-ref.supabase.co',
  anonKey: 'public-anon-key'
};
